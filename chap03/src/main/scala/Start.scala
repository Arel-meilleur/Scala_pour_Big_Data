object Start {

  def main(args: Array[String]){

    // Exo 1.1
    /**
     * Create a variable of type Int, assign a value, and then see what
        methods are available for that type. Repeat the same process for
        the String variable.
     */
        //  Pour les entiers
        var entier :Int = 1;
        println("Mon entier est "+ entier);

            // Les méthodes quis sont utilisable sur les entiers sont : entier.equals() ; entier.equals(); entier.toString() ...

        //  Pour les Chaines de caractères
        var chaine : String = "Arel";
            // Les méthodes quis sont utilisable sur les chaines de caractères sont : chaine.equals() ; chaine.trim(); ...

        println("Ma chaien est "+ chaine);
  }
}
